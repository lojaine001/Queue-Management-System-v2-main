// Requests go through the Vite dev-server proxy → localhost:8000
// To point at a different backend, set VITE_API_TARGET in a .env file
export const API_URL = '/api';
export const REFRESH_MS = 15000;
export const HEADERS = {};
